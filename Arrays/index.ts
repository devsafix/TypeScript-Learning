// Example of an array of numbers
let arr1: number[] = [1, 2, 43, 42]; // Declares an array containing numbers

// Example of an empty array and dynamically adding elements
let arr2: number[] = []; // Declares an empty array for numbers
arr2[0] = 10;            // Assigns the value 10 to the first index
arr2.push(20);           // Adds the value 20 to the end of the array

// Example of a string array
let nameOfTeachers: string[] = ["Safi", "Rafi", "Mafi"]; // Array of teacher names (strings)

// Example of a number array
let ageOfTeachers: number[] = [30, 35]; // Array of teacher ages (numbers)

// Example of a two-dimensional array (array of arrays)
let twoDArr: number[][] = [
    [1, 2, 1],   // First row
    [1, 20, 1],  // Second row
    [1, 30, 1],  // Third row
];
